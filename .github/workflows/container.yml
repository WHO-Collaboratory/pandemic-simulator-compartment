name: container

on:
  push:
    branches: []  # no branches → never triggers
  workflow_dispatch: 

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: unreachable step
        run: echo "unreachable"
